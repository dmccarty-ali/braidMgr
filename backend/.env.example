# =============================================================================
# braidMgr Backend Environment Variables
# =============================================================================
# Copy this to .env and customize for your environment
#
# For local development (non-Docker):
#   cp .env.example .env
#   source .env
#   uvicorn src.api.main:app --reload

# =============================================================================
# Database Connection
# =============================================================================
DB_HOST=localhost
DB_PORT=5434
DB_NAME=braidmgr_dev
DB_USER=postgres
DB_PASSWORD=postgres

# =============================================================================
# Application Settings
# =============================================================================
ENVIRONMENT=development
LOG_LEVEL=DEBUG

# =============================================================================
# Chat/AI Configuration
# =============================================================================
# CHAT_MODE options:
#   cli  - Use Claude CLI (requires 'claude' in PATH, uses your subscription)
#   api  - Use Anthropic API (requires ANTHROPIC_API_KEY)
#
# For local development, use 'cli' to leverage your Claude subscription
# For production/cloud, use 'api' with an API key
CHAT_MODE=cli

# Required only if CHAT_MODE=api
# ANTHROPIC_API_KEY=sk-ant-...
